const myObject = {
    js: "java script",
    coa: "computer organigation arcitactur"
}

for (const key  in myObject){

//    console.log(`${key} full form is ${myObject[key]}`);
    
}

const subject = [ "coa","dstl","math 4","ds"]

for (const key in subject) {
    // console.log(subject[key]);   
}

